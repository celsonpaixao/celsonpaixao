export default interface ExperienceType {
    image: string;
    title: string;
    nivel: string;
    competence: string[];
    type_local: string;
    start: string;
    End: string;
    atual: boolean
}